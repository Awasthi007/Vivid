<link rel="stylesheet" href="/css/user_profile.css">
    <h1>
        This is the profile page
    </h1>

    <img id="profile_image" src="<%= user.avatar %>" alt="<%= user.name %>" width="300px">

   <p class="user_details"><%= profile_user.name %></p>
   <p class="user_details"><%= profile_user.email %></p>

   <% if(locals.user.id == profile_user.id) {%>
        <!-- if user matches then show the form else show profile info -->
        <form action="/users/update/<%= profile_user.id %>" enctype="multipart/form-data" method="post">
            <input type="text" name="name" placeholder="Your name" value="<%= profile_user.name%>" required>
            <input type="text" name="email" placeholder="Your Email" value="<%= profile_user.email%>" required>
            <input type="file" name="avatar" placeholder="Profile Picture">
            <input type="submit" value = "Update">
        </form>

    <% } else {%>
    <%}%>